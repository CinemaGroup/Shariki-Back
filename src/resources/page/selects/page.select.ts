import { blockSelect } from 'src/resources/block/select/block.select'
import { seoSelect } from 'src/resources/seo/select/seo.select'

export const pageSelect = {
	blocks: {
		select: blockSelect
	},
	seo: {
		select: seoSelect
	}
}